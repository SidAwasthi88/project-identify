# Face encodings (biometric data - never commit real student faces)
data/encodings/*.pkl
data/embeddings/*.pkl

# SQLite leftovers (if any remain from before the MySQL migration)
data/*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Environment variables / credentials
.env

# Streamlit
.streamlit/secrets.toml

# OS / editor junk
.DS_Store
.vscode/
.idea/

# Exported reports (usually regenerated, don't need to be in version control)
*.csv
attendance_subject_*.csv
attendance_session_*.csv
tempCodeRunnerFile.py
